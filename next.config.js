/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'],
    },
    reactStrictMode: true,
    swcMinify: true,
  };
  
  module.exports = nextConfig;